import React from 'react';

const Packages: React.FC = () => {
    return (
        <div style={{ height: '800px' }}>
            <h1>Packages Component</h1>
        </div>
    );
};

export default Packages;